---
import { clubConfig, languages, type Lang } from '../lib/club.config';
import type { Translations } from '../lib/i18n/types';
import ArmenianFlag from './ArmenianFlag.astro';

interface Props {
  lang: Lang;
  t: Translations;
}

const { lang, t } = Astro.props;
---

<header class="fixed top-0 left-0 right-0 z-50 bg-river-bg/85 backdrop-blur-md border-b border-river-border">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16 lg:h-20">
      <a href={lang === 'hy' ? '/' : `/${lang}/`} class="flex items-center gap-3 group">
        <img
          src="/logo.jpg"
          alt="Riverok Club"
          class="h-12 w-12 rounded-lg object-cover ring-1 ring-river-border"
          width="48"
          height="48"
        />
        <ArmenianFlag class="w-5 h-3.5 rounded-sm shadow-sm ring-1 ring-river-border/50" />
      </a>

      <nav class="hidden lg:flex items-center gap-8 text-sm font-medium text-river-muted">
        <a href="#how-to-start" class="hover:text-river-accent-bright transition-colors">{t.nav.howToStart}</a>
        <a href="#tournaments" class="hover:text-river-accent-bright transition-colors">{t.nav.tournaments}</a>
        <a href="#calculator" class="hover:text-river-accent-bright transition-colors">{t.nav.calculator}</a>
        <a href="#deposit" class="hover:text-river-accent-bright transition-colors">{t.nav.deposit}</a>
        <a href="#support" class="hover:text-river-accent-bright transition-colors">{t.nav.support}</a>
      </nav>

      <div class="flex items-center gap-3">
        <div class="hidden sm:flex items-center gap-1 bg-river-surface rounded-full p-1 border border-river-border">
          {
            languages.map((l) => (
              <a
                href={l.href}
                class:list={[
                  'px-2.5 py-1 rounded-full text-xs font-semibold transition-all',
                  l.code === lang
                    ? 'bg-river-accent text-river-bg shadow-sm'
                    : 'text-river-muted hover:text-river-text',
                ]}
              >
                {l.label}
              </a>
            ))
          }
        </div>

        <a
          href={clubConfig.telegram.botUrl}
          target="_blank"
          rel="noopener noreferrer"
          data-track="InitiateCheckout"
          class="hidden sm:inline-flex items-center px-4 py-2 btn-primary text-sm rounded-full"
        >
          {t.nav.ctaDeposit}
        </a>

        <button
          id="mobile-menu-btn"
          class="lg:hidden p-2 text-river-muted hover:text-river-text"
          aria-label="Menu"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <div id="mobile-menu" class="hidden lg:hidden border-t border-river-border bg-river-bg/95">
    <nav class="flex flex-col px-4 py-4 gap-3 text-sm font-medium text-river-muted">
      <a href="#how-to-start" class="py-2 hover:text-river-accent-bright">{t.nav.howToStart}</a>
      <a href="#tournaments" class="py-2 hover:text-river-accent-bright">{t.nav.tournaments}</a>
      <a href="#calculator" class="py-2 hover:text-river-accent-bright">{t.nav.calculator}</a>
      <a href="#deposit" class="py-2 hover:text-river-accent-bright">{t.nav.deposit}</a>
      <a href="#support" class="py-2 hover:text-river-accent-bright">{t.nav.support}</a>
      <div class="flex gap-2 pt-2">
        {
          languages.map((l) => (
            <a
              href={l.href}
              class:list={[
                'px-3 py-1.5 rounded-full text-xs font-semibold',
                l.code === lang ? 'bg-river-accent text-river-bg' : 'bg-river-surface text-river-muted border border-river-border',
              ]}
            >
              {l.label}
            </a>
          ))
        }
      </div>
      <a
        href={clubConfig.telegram.botUrl}
        target="_blank"
        rel="noopener noreferrer"
        data-track="InitiateCheckout"
        class="mt-2 text-center px-4 py-3 btn-primary rounded-full"
      >
        {t.nav.ctaDeposit}
      </a>
    </nav>
  </div>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  btn?.addEventListener('click', () => menu?.classList.toggle('hidden'));
  menu?.querySelectorAll('a').forEach((a) => {
    a.addEventListener('click', () => menu?.classList.add('hidden'));
  });
</script>
